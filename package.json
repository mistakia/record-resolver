{
  "name": "record-resolver",
  "version": "0.0.1",
  "description": "Resolver for Record: a distributed system for audio files",
  "main": "index.js",
  "dependencies": {
    "youtube-dl": "^3.0.2"
  },
  "devDependencies": {
    "chai": "4.3.0",
    "mocha": "8.3.0",
    "nyc": "^15.1.0",
    "yargs": "^11.0.0"
  },
  "scripts": {
    "test": "$(npm bin)/mocha --reporter spec --timeout 60000 test/*.js",
    "coverage": "$(npm bin)/nyc $(npm bin)/mocha -- -R spec --timeout 60000 test/*.js"
  },
  "author": "",
  "license": "MIT"
}
